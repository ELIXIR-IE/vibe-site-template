---
import Page from "../layouts/Page.astro";
import DateList from "../components/DateList.astro";
import { timeline, pageLabel } from "../lib/data";
---

<Page title={pageLabel("key-dates")}>
  <div class="key-dates"><DateList items={timeline} /></div>
</Page>

<style>
  .key-dates { max-width: 640px; }
</style>
