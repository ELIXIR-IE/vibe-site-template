---
import Page from "../layouts/Page.astro";
import { faq, site, pageLabel } from "../lib/data";
import { inlineMarkdown } from "../lib/url";
---

<Page title={pageLabel("faq")}>
  <div class="faq">
    {faq.map((f) => (
      <details>
        <summary>{f.q}</summary>
        <p set:html={inlineMarkdown(f.a)} />
      </details>
    ))}
  </div>
  {site.event.contact_email && (
    <p class="faq-contact">Contact: <a href={`mailto:${site.event.contact_email}`}>{site.event.contact_email}</a></p>
  )}
</Page>

<style>
  .faq { max-width: 780px; border-top: 1px solid var(--c-border); }
  details { border-bottom: 1px solid var(--c-border); }
  summary {
    cursor: pointer;
    list-style: none;
    display: flex;
    justify-content: space-between;
    gap: var(--space-4);
    padding: var(--space-4) 0;
    font-weight: 600;
    font-size: 1.05rem;
  }
  summary::-webkit-details-marker { display: none; }
  summary::after {
    content: "+";
    flex: none;
    width: 1.6rem;
    height: 1.6rem;
    display: grid;
    place-items: center;
    border-radius: 50%;
    background: var(--c-primary-soft);
    color: var(--c-primary);
    font: 700 1.1rem / 1 var(--f-body);
  }
  details[open] summary::after { content: "–"; background: var(--c-accent-soft); color: var(--c-cta); }
  details p { padding-bottom: var(--space-4); max-width: 65ch; }
  .faq-contact { margin-top: var(--space-6); }
</style>
